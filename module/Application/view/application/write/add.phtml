<h1>Add a new instruction</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url());
echo $this->partial('application/write/form', [
    'form' => $form,
    'submitLabel' => 'Insert new instruction',
]);